{
  "name": "pclabs",
  "scripts": {
    "scaffold": "node ./packages/creditor/cli --pattern=package --action=create --location=/"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "eslint": "^6.8.0",
    "lerna": "^3.20.2",
    "mocha": "^7.1.1"
  }
}
